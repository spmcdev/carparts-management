[variables]
NODE_ENV = "production"
NPM_CONFIG_CACHE = "/tmp/.npm"

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm cache clean --force", "npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -l $PORT"
